{
  "name": "diff-curl",
  "version": "1.0.2",
  "bin": {
    "diff-curl": "./index.js"
  },
  "scripts": {
    "test": "yarn run lint --max-warnings 0",
    "lint": "eslint --cache --fix index.js",
    "lint:fix": "yarn lint --fix"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NickHeiner/diff-curl.git"
  },
  "dependencies": {
    "jest-diff": "^24.9.0",
    "lodash": "^4.17.15",
    "yargs": "^15.0.2"
  },
  "eslintConfig": {
    "env": {
      "es6": true,
      "node": true
    },
    "extends": "nth"
  },
  "devDependencies": {
    "eslint": "^6.7.1",
    "eslint-config-nth": "^2.0.1"
  }
}
